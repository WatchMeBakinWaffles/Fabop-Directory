{% extends 'baselayout.html.twig' %}

{% block title %}EntityTags{% endblock %}

{% block content %}
<div class="col">
    <h1>EntityTags</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity_tag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity_tag.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('entity_tags_index') }}">back to list</a>

    <a href="{{ path('entity_tags_edit', {'id': entity_tag.id}) }}">edit</a>

    {{ include('entity_tags/_delete_form.html.twig') }}
</div>
{% endblock %}
