{% extends 'base.html.twig' %}

{% block title %}TagsAffect index{% endblock %}

{% block body %}
    <h1>TagsAffect index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tags_affect in tags_affects %}
            <tr>
                <td>{{ tags_affect.id }}</td>
                <td>
                    <a href="{{ path('tags_affect_show', {'id': tags_affect.id}) }}">show</a>
                    <a href="{{ path('tags_affect_edit', {'id': tags_affect.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('tags_affect_new') }}">Create new</a>
{% endblock %}
