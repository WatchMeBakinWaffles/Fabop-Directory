{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button id='validForm' class="btn btn-primary m-1"><i class="fas fa-check"></i>{{ button_label|default(' Valider') }}</button>
{{ form_end(form) }}
