{% extends 'baselayout.html.twig' %}

{% block title %}EntityShows{% endblock %}

{% block content %}
    <h1>EntityShows</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity_show.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity_show.name }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ entity_show.year }}</td>
            </tr>
            <tr>
                <th>Sheet_id</th>
                <td>{{ entity_show.sheetId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('entity_shows_index') }}">back to list</a>

    <a href="{{ path('entity_shows_edit', {'id': entity_show.id}) }}">edit</a>

    {{ include('entity_shows/_delete_form.html.twig') }}
{% endblock %}
