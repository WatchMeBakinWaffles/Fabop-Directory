{% extends 'baselayout.html.twig' %}

{% block title %}Import-export de données{% endblock %}

{% block content %}
    <section class="row">
        <section class="col-6">
            <h2><i class="fa fa-file-import"></i> Importer des données</h2>
            <form action="{{ path('manager/import') }}" enctype="multipart/form-data" method="POST">
                <div class="custom-file mb-1">
                    <label class="custom-file-label" id="labelChoose" for="import-file">Choose file</label>
                    <input type="file" class="custom-file-input" id="import-file" name="import">
                </div>
                <button type="submit" class="btn btn-success"><i class="fa fa-check"></i> Importer</button>
            </form>
        </section>

        <section class="col-6">
            <h2><i class="fa fa-upload"></i> Exporter des données</h2>
            <form action="{{ path('manager/export') }}">
                <button type="submit" class="btn btn-success"><i class="fa fa-check"></i> Exporter</button>
            </form>
        </section>
    </section>
{% endblock %}