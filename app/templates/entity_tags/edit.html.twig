{% extends 'baselayout.html.twig' %}

{% block title %}Édition du TAG "{{ entity_tag.name }}"{% endblock %}

{% block content %}
    <h2><i class="fas fa-edit"></i> Édition du TAG "{{ entity_tag.name }}"</h2>

    {{ include('entity_tags/_form.html.twig', {'button_label': 'Valider'}) }}

    {{ include('entity_tags/_delete_form.html.twig') }}
{% endblock %}
