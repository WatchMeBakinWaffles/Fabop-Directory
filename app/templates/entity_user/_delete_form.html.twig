<form method="post" action="{{ path('admin_user_delete', {'id': entity_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entity_user.id) }}">
    <button class="btn">Supprimer</button>
</form>
